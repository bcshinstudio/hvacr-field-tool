import fs from"fs";import{buildWicKnowledgeFacts}from"../src/knowledge/wic_fact_adapter.js";import{evaluateKnowledge}from"../src/knowledge/knowledge_engine.js";const J=p=>JSON.parse(fs.readFileSync(new URL(p,import.meta.url),"utf8"));const rules=J("../data/knowledge/core/evidence_rules.json"),relationships=J("../data/knowledge/core/relationships.json"),checks=J("../data/knowledge/core/checks.json"),discriminators=J("../data/knowledge/core/discriminators.json"),application=J("../data/knowledge/applications/walk_in_cooler.json");const run=calculated=>{const a=buildWicKnowledgeFacts({calculated,references:{evaporatorSuperheat:application.reference_profiles.evaporatorSuperheat.fallback,condenserSubcooling:null},observations:{},derived:{}});return{a,r:evaluateKnowledge({facts:a.facts,rules:rules.items,relationships:relationships.items,checks:checks.items,discriminators:discriminators.items})}};let x=run({evaporatorSuperheat:25});if(!x.a.facts.includes("FACT_EVAP_SH_HIGH")||!x.r.conditions.some(v=>v.id==="COND_EVAP_STARVED")||x.r.diagnoses.length||x.r.nextCheck!=="CHECK_MEASURE_SC")throw Error("high-SH branch failed");x=run({evaporatorSuperheat:25,condenserSubcooling:10.2});if(!x.a.facts.includes("FACT_SUBCOOLING_MEASURED")||x.r.nextCheck==="CHECK_MEASURE_SC")throw Error("measured-SC branch failed");console.log("PASS: high-SH starvation and measured-but-unclassified SC workflow.");